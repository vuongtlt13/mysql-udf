[workspace]
resolver = "2"
members = [
    "udf-blake",
    "udf-net",
    "udf-jsonify",
    "udf-lipsum",
    "udf-uuid",
    "udf-xxhash",
    "test-integration",
]

[profile.release]
# Minimize binary size by removing all symbols
strip = "symbols"
codegen-units = 1
lto = "fat"
