[workspace]
resolver = "2"
members = [
    "udf-jsonify",
    "udf-lipsum",
    "udf-uuid",
    "udf-xxhash",
    "test-integration",
]

[profile.release]
# Minimize binary size by removing all symbols
lto = "fat"
